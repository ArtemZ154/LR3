# Этот файл настраивает проверки для clang-tidy.
# Он написан на языке YAML.

Checks: >
  -*,
  clang-analyzer-*,
  bugprone-*,
  performance-*,
  modernize-*,
  readability-*

# >- означает, что мы сначала отключаем все проверки (-*),
# а затем включаем целые группы проверок (например, clang-analyzer-*).
#
# - clang-analyzer-*: Поиск серьезных логических ошибок (утечки памяти, null-дереференс).
# - bugprone-*: Поиск конструкций, которые часто приводят к ошибкам.
# - performance-*: Поиск узких мест в производительности.
# - modernize-*: Советы по обновлению кода до современных стандартов C++ (C++11/14/17).
# - readability-*: Улучшение читаемости кода.

HeaderFilterRegex: '.*'
# Эта опция говорит clang-tidy применять проверки ко всем заголовочным файлам,
# которые он встречает в вашем проекте.
